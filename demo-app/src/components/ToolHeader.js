export const ToolHeader = ({ headerText }) => {

  return (
    <header>
      <h1>{headerText}</h1>
    </header>    
  );

};

// export const ToolHeader = (props) => {

//   return (
//     <header>
//       <h1>{props.headerText}</h1>
//     </header>    
//   );

// };